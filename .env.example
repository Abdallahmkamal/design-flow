# Local development defaults. Copy to .env.local and replace only with
# environment-appropriate publishable values.
VITE_APP_ENV=local
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-key
