# Design assets

`design-flow-mark.svg` is the exact exported Design Flow brand mark used by the
approved team-ready shell in Figma file `2c9QoPS8BLcTdiIoBEeeni`, desktop node
`105:5824` and mobile node `105:12378`. It is stored locally because Figma MCP
asset URLs expire and must not be used as runtime dependencies.
