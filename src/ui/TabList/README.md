# TabList

Controlled Design Flow tab navigation. `items` require stable values, labels,
and panel IDs. The selected tab is the single tab stop; Left/Right wrap and
Home/End move and activate. Panels remain feature-owned and URL state remains
the page's responsibility.
