# Checkbox

Design Flow-owned independent boolean field.

## Public API

- required visible `label`
- optional `description` and `error`
- standard native checkbox attributes and forwarded ref

The visible label is part of the activation target.
