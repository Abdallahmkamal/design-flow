# Badge

Design Flow-owned read-only metadata.

## Public API

- `tone`: `neutral`, `info`, `success`, `warning`, or `error`
- standard span attributes

Badges always contain visible text and never act as controls.
