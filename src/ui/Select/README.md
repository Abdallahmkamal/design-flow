# Select

Design Flow-owned bounded single-choice field.

## Public API

- required `label`
- optional `description`, `error`, and `hideLabel`
- standard native select attributes and forwarded ref

The component preserves native selection and keyboard behavior.
